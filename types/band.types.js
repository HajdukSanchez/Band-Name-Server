const GET_BANDS = 'bands' // Event to get all the bands
const ADD_VOTE = 'vote-band' // Event to add a vote to a band
const ADD_BAND = 'add-band' // Event to add a new band

module.exports = {
  GET_BANDS,
  ADD_VOTE,
  ADD_BAND,
}
